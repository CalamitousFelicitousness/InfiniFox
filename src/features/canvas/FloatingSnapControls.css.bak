/**
 * FloatingSnapControls.css
 * Extends shared toolbar patterns from toolbars.css
 */

.snap-controls {
  position: fixed;
  min-width: auto;
}

/* Snap toggle button with chevron */
.snap-toggle {
  display: flex;
  align-items: center;
  gap: var(--spacing-1);
}

.snap-chevron {
  width: 12px;
  height: 12px;
  opacity: 0.6;
}

/* Vertical panel that appears below toolbar */
.snap-panel {
  position: absolute;
  top: calc(100% + var(--spacing-1));
  left: 0;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-2);
  padding: var(--spacing-2);
  background: var(--custom-glass-bg);
  backdrop-filter: blur(var(--custom-glass-blur));
  -webkit-backdrop-filter: blur(var(--custom-glass-blur));
  border: 1px solid var(--custom-glass-border);
  border-radius: var(--radius-toolbar-base);
  box-shadow: var(--shadow-toolbar-floating);
  min-width: 160px;
  animation: slideDown var(--animation-fast) ease-out;
  z-index: 1;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-4px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Feature toggles - vertical layout */
.snap-features {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-1);
}

.snap-features .toolbar-item {
  width: 100%;
  justify-content: flex-start;
  gap: var(--spacing-2);
  padding: var(--spacing-1-5) var(--spacing-2);
}

.snap-item-label {
  font-size: var(--font-size-sm);
  flex: 1;
  text-align: left;
}

/* Settings section */
.snap-settings {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-2);
}

.snap-setting {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-1);
}

.snap-label {
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-medium);
  color: var(--color-text-tertiary);
  text-transform: uppercase;
  letter-spacing: var(--letter-spacing-wide);
}

.snap-select {
  padding: var(--spacing-1) var(--spacing-1-5);
  background: var(--color-surface-secondary);
  border: 1px solid var(--color-border-secondary);
  border-radius: var(--radius-sm);
  color: var(--color-text-primary);
  font-size: var(--font-size-xs);
  cursor: pointer;
  transition: var(--transition-colors);
  outline: none;
  width: 100%;
}

.snap-select:hover {
  border-color: var(--color-border-primary);
  background: var(--color-surface-hover);
}

.snap-select:focus {
  border-color: var(--color-primary-400);
  box-shadow: 0 0 0 2px var(--color-primary-100);
}

/* Panel separator */
.snap-panel-separator {
  width: 100%;
  height: 1px;
  background: var(--color-border-secondary);
  margin: var(--spacing-1) 0;
}

/* Presets section */
.snap-presets {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-1);
}

.snap-presets-label {
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-medium);
  color: var(--color-text-tertiary);
  text-transform: uppercase;
  letter-spacing: var(--letter-spacing-wide);
}

.snap-preset-buttons {
  display: flex;
  gap: var(--spacing-1);
}

.snap-preset {
  flex: 1;
  font-size: var(--font-size-xs);
  padding: var(--spacing-1) var(--spacing-1-5);
  min-width: auto;
}

/* Responsive adjustments */
@media (max-width: 640px) {
  .snap-panel {
    min-width: 140px;
  }
}
